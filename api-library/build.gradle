plugins {
  id "platform.java-conventions"
  id "org.springframework.boot"
}

group = "com.exatask.platform"
version = "1.0.0-SNAPSHOT"

bootJar {
  enabled = false
}

jar {
  enabled = true
}

dependencies {

  api (group: "org.springframework.boot", name: "spring-boot-starter-web") {
    exclude module: "spring-boot-starter-logging"
  }
  api group: "org.springframework.boot", name: "spring-boot-starter-log4j2"
  api group: "org.springdoc", name: "springdoc-openapi-ui", version: "${springdocOpenapiVersion}"
  api project(":i18n-library")
  api project(":logging-library")

  compileOnly group: "org.projectlombok", name: "lombok", version: "${lombokVersion}"
  annotationProcessor group: "org.projectlombok", name: "lombok", version: "${lombokVersion}"

  implementation group: "org.apache.commons", name: "commons-lang3", version: "${apacheCommonsVersion}"
  implementation project(":utilities-library")
}

publishing {
  publications {
    maven(MavenPublication) {

      artifactId = "api"
      from components.java
    }
  }
}
