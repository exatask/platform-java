plugins {
  id "platform.java-conventions"
  id "org.springframework.boot"
}

version = "1.0.0-SNAPSHOT"

bootJar {
  enabled = false
}

jar {
  enabled = true
}

dependencies {

  implementation project(":logging-library")
  implementation project(":utilities-library")

  compileOnly group: "org.springframework.boot", name: "spring-boot-starter-data-mongodb"
  compileOnly group: "com.github.mongobee", name: "mongobee", version: "${mongobeeVersion}"
  compileOnly group: "org.projectlombok", name: "lombok", version: "${lombokVersion}"

  annotationProcessor group: "org.projectlombok", name: "lombok", version: "${lombokVersion}"
}

publishing {
  publications {
    maven(MavenPublication) {

      artifactId = "exatask-migration"
      from components.java
    }
  }
}
